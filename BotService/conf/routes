# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

GET     /helloWorld                 controllers.Application.helloWorld


# Facebook messenger
POST        /fb-messenger-webhook               controllers.FbMessengerController.webhook
GET        /fb-messenger-webhook               controllers.FbMessengerController.verifyWebhook(`hub.mode`: Option[String], `hub.verify_token`: Option[String], `hub.challenge`: Option[String])

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

    